# Висп — дизайн-система

> Светлая матовая канва, крупные скруглённые карточки, одноголосая типографика на
> Onest и скупой тёплый акцент. Тёмные секции — не чёрные, а графитово-серые
> (`#2b2b2e`, взят из виньетки hero-видео). Иерархия строится размером и
> насыщенностью шрифта, а не приглушённым цветом.

**Тема:** светлая · **Источник истины:** `src/styles/global.css` (значения ниже
совпадают с токенами 1-в-1).

---

## 1. Жёсткие правила (не нарушать)

1. **Единый шрифт — Onest** на весь сайт. Иерархия — насыщенностью, не сменой
   семейства. Заголовки — Onest **900**, наборный текст — 400/500.
2. **Без CAPS.** Верхний регистр не используем нигде (ни `text-transform`, ни
   разреженный трекинг «под капс»). Только логотип-вордмарк «ВИСП» — исключение.
3. **Без серого и полупрозрачного ТЕКСТА.** Любой текст — сплошной, на полную
   непрозрачность: на светлом `#09090b` / `#18181b`, на тёмном `#ffffff`.
   Серый допустим только для НЕтекста — границы, фоны, разделители.
4. **Тени на карточки не вешаем.** Глубина — ступенями фона (canvas → card →
   muted → dark), а не `box-shadow`.
5. **Радиус карточек ≥ 28px** (обычно 36px). Меньше — ломает язык мягких
   контейнеров.
6. **Акцент `#e95931` — скупо.** Точечные интерактивные и смысловые моменты
   (одно слово в заголовке, hover, фокус). Не заливать им крупные площади и не
   делать им фон под текст мелким кеглем.
7. **Кнопки — простые сплошные пилюли.** Без обводок-колец и стеклянных бликов.

---

## 2. Цвета

| Токен | Значение | Роль |
|------|----------|------|
| `--color-obsidian` | `#09090b` | Основной текст на светлом, фон primary-кнопки, фон hero за видео |
| `--color-ink` | `#18181b` | Второй тёмный тон для текста |
| `--color-graphite-dark` | `#2b2b2e` | **Фон тёмных секций** (О компании, Проекты-стык, Заявка, футер, шапка) |
| `--color-graphite` | `#3f3f46` | Границы, обводки |
| `--color-accent` | `#e95931` | Фирменный тёплый акцент (скупо) |
| `--color-accent-hover` | `#d1481f` | Ховер-состояние акцента |
| `--color-slate` | `#52525b` | Плейсхолдеры в полях (нетекстовый серый) |
| `--color-steel` | `#71717a` | Служебный серый (границы/иконки) |
| `--color-ash` | `#a1a1aa` | Светлые штрихи, декоративные линии |
| `--color-pebble` | `#d4d4d8` | Тонкие разделители |
| `--color-fog` | `#ececee` | Приглушённая карточная поверхность (`--surface-muted`) |
| `--color-mist` | `#f4f4f5` | Канва страницы (`--surface-canvas`) |
| `--color-snow` | `#ffffff` | Белые карточки, поля ввода, светлые кнопки |

### Поверхности (ступени глубины)

| Уровень | Токен | Значение |
|---------|-------|----------|
| 1. Канва | `--surface-canvas` | `#f4f4f5` |
| 2. Карточка | `--surface-card` | `#ffffff` |
| 3. Приглушённая | `--surface-muted` | `#ececee` |
| 4. Тёмная секция | `--surface-dark` | `#2b2b2e` |

> Правило четырёх ступеней: в одной секции — не более четырёх фоновых тонов.

---

## 3. Типографика

**Шрифт:** `--font-body` = `--font-display` = `'Onest Variable'`
(самохостинг `@fontsource-variable/onest`, полная кириллица).

### Насыщенности
`--w-regular: 400` · `--w-medium: 500` · `--w-semibold: 600` · `--w-bold: 700` ·
`--w-black: 900`

- Заголовки (`h1–h4`): 900, `line-height: 1.02`, `letter-spacing: -0.01em`.
- Hero-заголовок (display): 900, `line-height: .92`, `letter-spacing: -0.02em`.
- Наборный текст: 400/500, `line-height: 1.55`.

### Шкала кегля

| Токен | Значение | Назначение |
|------|----------|------------|
| `--text-caption` | `0.75rem` (12px) | Метки, типы, подписи |
| `--text-body` | `1rem` (16px) | Основной текст, кнопки |
| `--text-body-lg` | `1.125rem` (18px) | Ведущие абзацы, лид |
| `--text-heading-sm` | `1.5rem` (24px) | Мелкие заголовки |
| `--text-heading` | `clamp(2rem, 4vw, 3rem)` | Заголовки |
| `--text-heading-lg` | `clamp(2.5rem, 6vw, 4.5rem)` | Заголовки секций |
| `--text-display` | `clamp(3rem, 9vw, 7.5rem)` | Hero |

---

## 4. Отступы, поля, сетка

### Шкала spacing
`4 · 8 · 12 · 16 · 20 · 24 · 28 · 32 · 40 · 48 · 64 · 80 · 120` px
(`--sp-4` … `--sp-120`, базовый шаг 4px).

### Раскладка
- **Макс. ширина страницы:** `--page-max: 1680px`.
- **Боковые поля контейнера:** `clamp(20px, 4vw, 56px)` (`.container`).
- **Вертикальный ритм секций:** `--section-gap: clamp(56px, 7vw, 96px)`
  (`.section { padding-block: var(--section-gap) }`).
- **Внутренние отступы карточек:** 24–28px.

### Радиусы

| Токен | Значение | Применение |
|------|----------|-----------|
| `--r-badge` | `12px` | Бейджи, чипы, метки |
| `--r-input` | `14px` | Поля ввода |
| `--r-compact` | `28px` | Компактные карточки (услуги) |
| `--r-card` | `36px` | Основные карточки, плитки |
| `--r-hero` | `48px` | Крупные тёмные панели, шапка-пилюля |
| `--r-pill` | `10000px` | Кнопки-пилюли, шапка |

---

## 5. Компоненты

### Кнопки (`.btn`, пилюля `--r-pill`, `padding: 16px 28px`, вес 600)
- `.btn--primary` — фон `#09090b`, текст белый. Простая сплошная пилюля.
- `.btn--ghost` — белый фон, текст `#09090b`, граница `1px --color-graphite`.
- `.btn--light` — белый фон, текст `#09090b` (для тёмных секций).
- Ховер: `translateY(-2px)` (`--ease-spring`). Никаких теней/колец.

### Карточка проекта / оборудования
Радиус 36px, `overflow: hidden`, без тени. Обложка — full-bleed изображение
(`object-fit: cover`), под ней тело с типом и названием. Ховер — подъём карточки
`translateY(-6px)` + лёгкий зум картинки `scale(1.04)`.

### Тёмная панель (О компании / Заявка)
Фон `#2b2b2e`, радиус `--r-hero` (48px), текст белый, акцентом — одно слово или
надзаголовок. Разделители — `rgba(255,255,255,0.16–0.18)`.

### Поле формы
Фон `#ffffff`, текст `#09090b`, радиус `--r-input` (14px), `padding: 14px 16px`,
плейсхолдер `--color-slate`, фокус — обводка `2px --color-accent`.

### Шапка (плавающая пилюля)
Фиксированная сверху, `--surface-dark` (`#2b2b2e`), радиус `--r-pill`, белый текст,
ссылки на ховере → акцент, справа белая кнопка-пилюля «Оставить заявку».

---

## 6. Структура главной страницы

1. **Hero** — видео-фон (`public/video/hero.{webm,mp4}` + постер), крупный
   заголовок «Свет, который *удивляет*» (акцентное слово), лид, CTA, статистика
   (10 лет / 14 дней / 1500+). Низ уходит в сплошной `#2b2b2e`.
2. **Наши проекты** — крупные кликабельные плитки-кейсы. Верх блока стартует с
   `#2b2b2e` (бесшовный стык с hero) и плавно уходит в канву.
3. **Оборудование** — витрина карточек светильников с реальными фото.
4. **О компании** — тёмная панель, текст + факты.
5. **Услуги** — 6 карточек (радиус 28px), номера 01–06 сплошным чёрным.
6. **Оставить заявку** — тёмная панель с формой + контакты.
7. **Футер** — тёмный, логотип, разделы, контакты.

---

## 7. Движение

- Микровзаимодействия: `transform` / `opacity`, 0.2–0.5s.
- Пружина входа/ховера: `--ease-spring`
  `cubic-bezier(0.175, 0.885, 0.32, 1.275)`.
- Уважать `prefers-reduced-motion` (анимации и ховер-сдвиги отключаются).
- Цвет/фон не анимируем.

---

## 8. Контакты
- Телефон: **+7 (495) 125-10-07**
