# АКБ Уфа — дизайн-система

> Глубокая чёрная канва вместо белой, тёплое золото вместо тёплого оранжевого.
> Дух прежний: одноголосая типографика на Onest, крупные скруглённые контейнеры,
> скупой акцент — только теперь акцент не заливка, а свечение и градиентная линия.
> Тёмное здесь не служебный элемент, а основа: чёрный → уголь → графит — три
> ступени одной и той же ночи. Светлые вставки (`#f7f5f0`) — редкие, для контраста
> и доверия (блок проверки АКБ, часть отзывов), не более одной-двух на страницу.

**Тема:** тёмная (премиум) · **Источник истины:** `src/styles/global.css`
(значения ниже — токены к реализации 1-в-1).

---

## 1. Жёсткие правила (не нарушать)

1. **Единый шрифт — Onest** на весь сайт. Иерархия — насыщенностью и кеглем, не
   сменой семейства. Заголовки — Onest **900**, огромный display на hero,
   узкий line-height. Наборный текст — 400/500.
2. **Без CAPS.** Верхний регистр не используем нигде — ни `text-transform`, ни
   разреженный трекинг «под капс». Логотип-вордмарк — единственное исключение.
3. **Без серого и полупрозрачного ТЕКСТА.** Любой текст — сплошной, на полную
   непрозрачность: на тёмном `#ffffff`, на редких светлых вставках `#0a0a0b`.
   Приглушённые тона — только для НЕтекста: границы, подложки, декоративные
   линии, иконки.
4. **`box-shadow` не используем вообще.** Глубина строится ступенями фона
   (void → coal → charcoal → graphite) и золотыми свечениями (размытые
   `radial-gradient`, а не тень). Свечение — не тень: у него нет чёрного
   компонента, только акцент на прозрачности.
5. **Радиус контейнеров ≥ 28px** (обычно 36px, крупные панели — 48px). Меньше —
   ломает язык мягких контейнеров.
6. **Акцент — золото/шампань, скупо.** Одно слово в заголовке, тонкая линия,
   ховер, цифра статистики, рамка-градиент в 1px. Никогда — заливка крупной
   площади сплошным золотом и никогда — золотой текст мелким кеглем на золотом
   фоне.
7. **Градиент — не украшение, а словарь с фиксированным набором.** Разрешены
   только: свечение за карточкой, подложка под фото товара, оверлей поверх
   hero-видео, тонкая рамка gold→transparent, текстовый градиент на акцентном
   слове/цифре. Свободных декоративных градиентов «для красоты» — нет.
8. **Кнопки — простые пилюли.** Первичная — золотая заливка-градиент с тёмным
   текстом, вторичная — прозрачная с 1px градиентной рамкой. Без стеклянных
   бликов, без колец, без тени.

---

## 2. Цвета

| Токен | Значение | Роль |
|------|----------|------|
| `--color-void` | `#0a0a0b` | Базовый фон страницы, самая глубокая ступень |
| `--color-coal` | `#131316` | Фон обычных секций (ступень 2) |
| `--color-charcoal` | `#1c1c20` | Поверхность карточек (ступень 3) |
| `--color-graphite` | `#26262b` | Приглушённая поверхность, поля ввода (ступень 4) |
| `--color-line` | `#35353c` | Границы, обводки не-текст |
| `--color-line-soft` | `rgba(255,255,255,.08)` | Тонкие разделители на тёмном |
| `--color-gold` | `#c9a962` | Базовый акцент — линии, рамки, ховеры |
| `--color-gold-bright` | `#d4af37` | Верхняя точка градиента, свечения, цифры |
| `--color-champagne` | `#e8dfc0` | Светлая точка текстовых градиентов, шиммер |
| `--color-gold-dim` | `#8a6f3a` | Нажатое/приглушённое состояние золота |
| `--color-smoke` | `#8f8f96` | Служебный серый — плейсхолдеры, вторичные иконки (НЕ текст) |
| `--color-ash` | `#c6c6cc` | Светлые декоративные штрихи на тёмном |
| `--color-paper` | `#f7f5f0` | Фон редких светлых вставок |
| `--color-paper-card` | `#ffffff` | Карточки внутри светлой вставки |
| `--color-ink` | `#0a0a0b` | Текст на светлой вставке |
| `--color-white` | `#ffffff` | Текст на тёмном |

### Поверхности (ступени глубины)

| Уровень | Токен | Значение |
|---------|-------|----------|
| 1. Канва | `--surface-void` | `#0a0a0b` |
| 2. Секция | `--surface-coal` | `#131316` |
| 3. Карточка | `--surface-charcoal` | `#1c1c20` |
| 4. Приглушённая / поле | `--surface-graphite` | `#26262b` |
| 5. Светлая вставка (редко) | `--surface-paper` | `#f7f5f0` |

> Правило четырёх ступеней в силе: в одной тёмной секции — не более четырёх
> фоновых тонов. Светлая вставка считается отдельной секцией и не смешивается
> с тёмными тонами внутри себя.

---

## 3. Градиенты (ключевой приём языка)

Фиксированный словарь — только эти пять рецептов, без импровизаций.

### 3.1 Свечение за карточкой (glow)
Размытое золотое пятно в углу карточки, элемент `::before`, `filter: blur(80–120px)`,
позиционирован под карточкой, не над контентом.
```
radial-gradient(60% 60% at 30% 20%,
  rgba(201,169,98,.18), transparent 70%)
```
Ховер — прозрачность пятна растёт до `.28`, без изменения размера.

### 3.2 Подложка под фото товара (АКБ)
Комбинация тёплого рефлекса снизу и холодного блика сверху — фото «стоит» на
свету, а не приклеено к фону.
```
radial-gradient(120% 120% at 50% 100%,
  rgba(201,169,98,.12), transparent 60%),
radial-gradient(80% 80% at 50% 0%,
  rgba(255,255,255,.05), transparent 70%)
```

### 3.3 Оверлей поверх hero-видео
Сверху — лёгкое затемнение для читаемости шапки, снизу — плавный переход в
сплошной фон страницы `#0a0a0b`.
```
linear-gradient(180deg,
  rgba(10,10,11,.55) 0%,
  rgba(10,10,11,.15) 32%,
  rgba(10,10,11,.6) 72%,
  #0a0a0b 100%)
```

### 3.4 Тонкая градиентная рамка (1px, gold → transparent)
Двойной фон: заливка на `padding-box`, градиент на `border-box`.
```
background:
  linear-gradient(var(--surface-charcoal), var(--surface-charcoal)) padding-box,
  linear-gradient(135deg, rgba(212,175,55,.65), rgba(212,175,55,0) 60%) border-box;
border: 1px solid transparent;
```
Используется на карточках, полях фокуса, шапке, чипах-бейджах.

### 3.5 Текстовый градиент (акцентное слово / цифра)
Только на одном слове заголовка или на числе статистики — никогда на целом
абзаце.
```
background: linear-gradient(90deg, #d4af37, #e8dfc0 50%, #c9a962);
-webkit-background-clip: text;
color: transparent;
```

---

## 4. Типографика

**Шрифт:** `--font-body` = `--font-display` = `'Onest Variable'`
(самохостинг `@fontsource-variable/onest`, полная кириллица).

### Насыщенности
`--w-regular: 400` · `--w-medium: 500` · `--w-semibold: 600` · `--w-bold: 700` ·
`--w-black: 900`

- Заголовки (`h1–h4`): 900, `line-height: 1.02`, `letter-spacing: -0.01em`.
- Hero-заголовок (display): 900, `line-height: .92`, `letter-spacing: -0.02em`.
- Наборный текст: 400/500, `line-height: 1.55`, цвет — сплошной белый (на
  тёмном) без исключений.

### Шкала кегля

| Токен | Значение | Назначение |
|------|----------|------------|
| `--text-caption` | `0.75rem` (12px) | Метки, типы, подписи, чипы |
| `--text-body` | `1rem` (16px) | Основной текст, кнопки |
| `--text-body-lg` | `1.125rem` (18px) | Ведущие абзацы, лид |
| `--text-heading-sm` | `1.5rem` (24px) | Мелкие заголовки, номера шагов |
| `--text-heading` | `clamp(2rem, 4vw, 3rem)` | Заголовки блоков |
| `--text-heading-lg` | `clamp(2.5rem, 6vw, 4.5rem)` | Заголовки секций |
| `--text-display` | `clamp(3rem, 9vw, 7.5rem)` | Hero |
| `--text-stat` | `clamp(2.5rem, 5vw, 4rem)` | Крупные цифры статистики |

---

## 5. Отступы, поля, сетка

### Шкала spacing
`4 · 8 · 12 · 16 · 20 · 24 · 28 · 32 · 40 · 48 · 64 · 80 · 120` px
(`--sp-4` … `--sp-120`, базовый шаг 4px).

### Раскладка
- **Макс. ширина страницы:** `--page-max: 1680px`.
- **Боковые поля контейнера:** `clamp(20px, 4vw, 56px)` (`.container`).
- **Вертикальный ритм секций:** `--section-gap: clamp(56px, 7vw, 96px)`
  (`.section { padding-block: var(--section-gap) }`).
- **Внутренние отступы карточек:** 24–28px.

### Радиусы

| Токен | Значение | Применение |
|------|----------|-----------|
| `--r-badge` | `12px` | Бейджи, чипы (ёмкость Ач, гарантия) |
| `--r-input` | `14px` | Поля ввода |
| `--r-compact` | `28px` | Компактные карточки (преимущества) |
| `--r-card` | `36px` | Карточки товаров, кейсов |
| `--r-hero` | `48px` | Крупные панели, светлая вставка, шапка-пилюля |
| `--r-pill` | `10000px` | Кнопки-пилюли, шапка |

---

## 6. Компоненты

### Кнопки (`.btn`, пилюля `--r-pill`, `padding: 16px 28px`, вес 600)
- `.btn--primary` — фон `linear-gradient(135deg, #d4af37, #c9a962)`, текст
  `#0a0a0b` (сплошной, не приглушённый). Ховер — shimmer-развёртка блика (см.
  §8) + magnetic-смещение к курсору.
- `.btn--ghost` — прозрачная заливка, 1px рамка gold→transparent (§3.4), текст
  белый. Ховер — рамка становится непрозрачнее, текст получает текстовый
  градиент (§3.5).
- `.btn--dark` — для редких светлых вставок: фон `#0a0a0b`, текст белый.
- Ховер-подъём: `translateY(-2px)`, `--ease-spring`. Теней и колец нет.

### Карточка товара (АКБ)
Радиус 36px, `overflow: hidden`, фон `--surface-charcoal`, свечение §3.1 за
карточкой, фото на подложке §3.2. Тело карточки: бренд/модель, чипы «Ёмкость
XX Ач» и «Гарантия XX дней» (рамка §3.4), цена — крупная, цифра в текстовом
градиенте §3.5 только на цене со скидкой. Ховер — подъём `translateY(-6px)`,
зум фото `scale(1.04)`, усиление свечения до `.28`.

### Тёмная панель (Выкуп АКБ / Заявка)
Фон `--surface-coal`, радиус `--r-hero` (48px), текст белый, акцент — одно
слово или крупная цифра выкупной цены. Разделители — `--color-line-soft`.
Верхняя кромка панели — тонкая линия §3.4 на всю ширину.

### Светлая вставка (Как мы проверяем АКБ)
Фон `--surface-paper` (`#f7f5f0`), текст `--color-ink`, радиус `--r-hero`.
Используется точечно — как «включённый свет» на фоне ночи, не чаще одного
блока на страницу. Внутри — шаги диагностики карточками `--surface-paper-card`.

### Чип / бейдж
Прозрачная заливка, рамка §3.4, `--r-badge`, текст `--text-caption`, белый.
Для «Гарантия 6 мес.», «АГМ», «55–225 Ач», «Проверено на стенде».

### Счётчик статистики
Цифра `--text-stat`, вес 900, текстовый градиент §3.5, подпись под ней —
`--text-caption`, белая, сплошная. Анимация — count-up при входе в вьюпорт
(детали в §8).

### Поле формы
Фон `--surface-graphite`, текст белый, радиус `--r-input` (14px),
`padding: 14px 16px`, плейсхолдер `--color-smoke`, фокус — рамка §3.4 в
2px-версии (более яркая, `rgba(212,175,55,.9)`).

### Шапка (плавающая пилюля)
Фиксированная сверху, фон `--surface-charcoal`, тонкая верхняя кромка §3.4,
радиус `--r-pill`, белый текст, ссылки на ховере → золото (§3.5, только текст
ссылки). Справа — золотая кнопка-пилюля «Оставить заявку».

---

## 7. Структура главной страницы

1. **Hero** — фоновое видео на весь первый экран (диагностика/установка АКБ в
   боксе, замер нагрузочной вилкой), поверх — оверлей §3.3 (лёгкое затемнение
   сверху, переход в `#0a0a0b` снизу). Заголовок display: «Б/У аккумуляторы
   с&nbsp;{ГАРАНТИЕЙ}» — акцентное слово в текстовом градиенте §3.5. Лид —
   одна строка о проверке на стенде и подборе по авто. CTA `.btn--primary`
   «Подобрать АКБ» + `.btn--ghost` «Проверить в наличии». Строка цифр
   (лет на рынке / проверенных АКБ / дней гарантии / отдано на выкуп) —
   каждая цифра со счётчиком §8.
2. **Почему Б/У выгодно** — 4 компактные карточки (`--r-compact`): экономия
   до 50–70% против нового, каждая единица прошла диагностику, экология
   (вторая жизнь вместо утилизации), гарантия как у нового.
3. **Каталог / категории** — плитки: «Легковые», «Грузовые и спецтехника»,
   «По ёмкости (55–225 Ач)», «По бренду». Карточки товара — как в §6, со
   свечением и подложкой под фото.
4. **Как мы проверяем АКБ** — светлая вставка (редкое включение света):
   шаги диагностики — нагрузочная вилка, тест ёмкости, контрольный цикл
   заряда/разряда, визуальный осмотр корпуса и клемм, итоговый талон
   гарантии. Шаги — пронумерованы 01–05, номер сплошным `--color-ink`.
5. **Выкуп старых АКБ** — тёмная панель: как сдать старый аккумулятор в зачёт
   или на выкуп, ориентир цены за кг/тип, форма-калькулятор или CTA
   «Узнать цену выкупа».
6. **Отзывы и цифры** — карточки отзывов (`--surface-charcoal`) вперемешку с
   крупными счётчиками §6 (АКБ в наличии, средний срок службы, повторные
   клиенты, % возвратов по гарантии — обратный счётчик, стремящийся к нулю).
7. **Заявка и контакты** — тёмная панель с формой (модель авто → подбор АКБ)
   и контактами: {PHONE}, {ADDRESS}, Уфа. Карта — плейсхолдер-блок с рамкой
   §3.4.
8. **Футер** — тёмный, `--surface-void`, вордмарк, разделы (каталог, услуги,
   контакты), соцсети, юридическая строка.

---

## 8. Движение

Анимации — обоснованные и «дорогие»: медленные, с экспоненциальным затуханием,
без прыгучести там, где не нужен акцент. Все анимации уважают
`prefers-reduced-motion: reduce` — при нём reveal-каскады и parallax
отключаются, элементы появляются сразу с `opacity: 1`, shimmer и magnetic-ховер
не работают.

- **Reveal секций при скролле** — `translateY(28px) → 0` + `opacity 0 → 1`,
  `duration: .7s`, `ease: cubic-bezier(.16,1,.3,1)` (expo-out). Дочерние
  элементы каскадом с задержкой `70–90ms` между соседями (карточки в сетке,
  строки цифр).
- **Parallax hero-видео** — при скролле видео смещается на `translate3d`
  с коэффициентом `.15–.2` от прокрутки (эффект «видео чуть отстаёт»),
  `will-change: transform`, без анимации — прямое связывание со scroll offset,
  сглаживание через `requestAnimationFrame`.
- **Счётчики цифр** — при входе блока в вьюпорт число считается от 0 (или от
  верхней границы для обратных счётчиков) до целевого значения,
  `duration: 1.2–1.6s`, `ease: cubic-bezier(.22,1,.36,1)`, форматирование с
  разрядами.
- **Shimmer по золотым линиям/рамкам** — узкая световая полоса пробегает по
  градиентной рамке (`background-position: -100% → 200%`), `duration: 2.8s`,
  `ease: linear`, срабатывает на ховере карточки/кнопки либо как редкий
  «вдох» раз в 6–8с на акцентном слове hero — ненавязчиво, не более одного
  одновременного shimmer-элемента в области видимости.
- **Magnetic-ховер кнопок** — кнопка смещается к курсору в пределах своих
  границ, максимум `6–10px` по каждой оси, привязка к позиции курсора без
  анимации (прямое отслеживание), возврат в исходное положение при уходе
  курсора — `duration: .35s`, `--ease-spring: cubic-bezier(.34,1.56,.64,1)`.
- **Цвет и фон не анимируем** долгими transition — только мгновенные
  hover-переходы состояний, `≤ .2s`, `ease-out`. Никаких плавных «заливок»
  фона по 1–2 секунды.

---

## 9. Контакты
- Телефон: **{PHONE}**
- Адрес: **{ADDRESS}**, Уфа
